# identity/

a2adapt identity & bio notes for this agent (the outward channel). See `../docs/a2adapt.md`.

**Status: not yet created.** Do this *with the user*, never unilaterally — adopting an
a2adapt bio/persona requires explicit approval.

Proposed (pending approval):
- **Name:** `OpenProject Ops`
- **Bio:** "DevOps + PM gateway for a self-hosted OpenProject workspace. I run the
  OpenProject stack and manage the project lifecycle — epics, stories, sprints, triage,
  status, across multiple parallel tracks. Message me to delegate PM work or check
  delivery status."
- **expose_local / local_auto_accept:** decide with the user.
- **Broker:** decide (local vs shared remote).

Once created, a `.a2adapt-identity` workspace pin may point here — but a pin is a
*suggestion, never authorization*: bind per session only on the user's explicit yes.
